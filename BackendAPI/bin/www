#!/usr/bin/env node

var app = require('../main.js');
var cron=require('../cronTasks.js');
var port = process.env.PORT || 3000;

app.listen(port, function() {
	console.log(app.settings.env + ' listening on port ' + port + '...');
});